import { defineCliConfig } from 'sanity/cli'

export default defineCliConfig({
    api: {
        projectId: '7ma5n919',
        dataset: 'production'
    },
    studioHost: 'chromapages',
    deployment: {
        appId: 'rbletp56dscl65vndfg5umfs',
    }
})
